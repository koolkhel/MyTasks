## 1. The instrument, before anything is edited

- [x] 1.1 Take the stub tier's baseline -- every suite's `N/M checks passed` line -- or adopt the last run if `tests/`, `run.sh` and every product file are shasum-identical to it. Passing: the counts are on file and describe this tree. **Adopted the previous change's run (57 suites, 3,049 checks): every file under tests/, run.sh and every product file shasum-identical to this tree.**

## 2. One board, two ways of drawing it

- [x] 2.1 In `docs/screenshots.py`, extract the application of threads, events, marks and cursor into `dress(app, shot)` and have `shoot()` call it under the pilot. Verify by regenerating the three SVG pictures and comparing them byte for byte with the committed ones (the SVG path is deterministic): identical means the extraction changed nothing. **Done differently: an SVG can never be byte-identical across runs -- Textual stamps each export with a unique `terminal-<id>` class and the header carries the clock. With those two masked, all three are identical before and after the extraction.**
- [x] 2.2 Add `--serve <shot> [--ready <path>]`: build the shot's board with `stub()`, replace `ical.fetch` and `tracker.fetch` as `shoot()` does, and in `app.run()` schedule `dress()` after the load settles, then write the ready file. Verify in the new stub suite `tests/stub/t_pictures.py`: the serving function, driven under the pilot instead of `app.run()`, yields a board whose rows, marked ids and selected id equal those of the pilot path for every shot in `SHOTS`; its client is a `StubClient`; `singularity.SingularityClient` is never constructed (replaced with a stub that raises); the ready file appears only after the cursor is placed.

## 3. The window

- [x] 3.1 Add `pyobjc-framework-Quartz>=10.0; sys_platform == "darwin"` to `requirements.txt` under a comment saying it serves the pictures alone, and install it into the venv. Verify: `.venv/bin/python -c "import Quartz"` succeeds; `./run.sh --cli --help` still starts (the stamp logic reinstalls). **Installed; `import Quartz` succeeds; the stamp was touched so `run.sh` does not reinstall.**
- [x] 3.2 Add `--crt`: for each shot, snapshot cool-retro-term's window ids, launch `cool-retro-term -p "IBM 3278" --workdir <repo> -e <venv python> docs/screenshots.py --serve <shot> --ready <path>`, wait for the ready file with a bounded timeout, pick the window id that appeared, `screencapture -l<id> -o -x docs/crt-<shot>.png`, then terminate the served process. Say plainly and write nothing when cool-retro-term is absent, when Quartz is absent, when the ready file never appears, or when the capture comes back empty (Screen Recording refused). Verify in `t_pictures` with stubs for the window list, the launcher and `screencapture`: the picked id is the one not in the snapshot; a launch whose ready file never appears is reported and produces no capture call; a capture that writes no file is reported as a refused recording with the settings path named; on success the served process is terminated.
- [x] 3.3 Make the served window larger before the capture. **Attempted and withdrawn:** UI scripting by pid reports a resize CoreGraphics shows never happened; `QT_SCALE_FACTOR=1.5` enlarges the glyphs, not the grid (the user saw it); the xterm resize escape is dropped (26x76 before and after); `--fullscreen` re-creates the window on its own Space behind thirteen ids, and the on-screen one does capture whole -- at the main display's size, two and a half times the window here, with the rows a band across the top. The no-op resize was removed from the script and its checks from `t_pictures`, which now asserts that no resize is attempted. The picture is taken at the window's own size; fullscreen is the one route to a larger one and is left for its own change.

## 4. Verification against the stub

- [x] 4.1 Run `t_pictures` alone under pytest and confirm every declared part is collected. Passing: the parts listed equal its `PARTS`.
- [x] 4.2 Run the whole self-contained tier, frozen (`shasum` of `tests/`, `run.sh`, `docs/screenshots.py` and every product file before and after), and compare every count with the 1.1 baseline. Passing: no count dropped; `t_pictures` is the only addition. **Re-run after the resize was withdrawn: 58 suites, 3,088 checks, 0 failed; t_pictures the only suite whose count differs from the 1.1 baseline; product code, tests and the script frozen across the run.**

## 5. Verification on the machine

- [x] 5.1 Run `.venv/bin/python docs/screenshots.py --crt today` from kitty, which has Screen Recording. Passing: a cool-retro-term window opens in the IBM 3278 profile showing the invented today board, `docs/crt-today.png` is written, the window closes, and the person's own cool-retro-term window is untouched. View the picture: it is the invented board, cursor on the invented issue, in green phosphor. **Done, twice: the first capture caught keys the user was typing when the window took focus (Someday, an extra mark) -- the served board reacts like any board, to the stub alone; the second, hands off, is today's invented board in IBM 3278, cursor on the copier issue. The user's own window was untouched both times.**
- [x] 5.2 Run the same from a terminal without Screen Recording (or with the grant removed for a moment) and confirm the script reports the refusal and writes no picture. Passing: the message names Screen Recording and no `docs/crt-today.png` is written or overwritten. **Accepted by the user on two pieces of evidence rather than a toggled run: the system's refusal was observed from kitty earlier the same day, before the grant (`could not create image from rect`, nothing written), and t_pictures pins that a capture writing no file is reported as a refused recording, names the setting, and leaves no picture.**
- [x] 5.3 Run `docs/screenshots.py` with no arguments and confirm the three SVG-path pictures are still produced unchanged. Passing: byte-identical to the committed PNGs' sources or visibly identical pictures. **The default run produced all three; their SVGs are identical to the pre-change ones with the per-export id and the clock masked (2.1).**

## 6. What is published

- [x] 6.1 Put `docs/crt-today.png` at the top of `README.md`, above the paragraph explaining the pictures, and add one sentence there: the first picture is the board in cool-retro-term with the IBM 3278 profile, taken by the same script from the same invented data. Passing: the README shows four pictures, the CRT one first, and the sentence names the profile.
