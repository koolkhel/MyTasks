## 1. Draw the bar

- [x] 1.1 Subclass the table so the row-style hook can be reached, and use the subclass where the board builds its list. Verify by running `t_fit.py` and `t_still.py` whole — both reach the table by type and by id, and are what would fail first if a subclass broke that. **Result: `TaskTable(DataTable)`. `t_fit` 37/37 and `t_still` 40/40, both unchanged -- a type query finds a subclass, which was checked with a running app before the subclass was written rather than assumed.**
- [x] 1.2 Give a marked row the selected row's background from that hook. Verify in `t_mark.py` by reading the style the table computes for each row: a marked row's differs from an unmarked row's in its background and in nothing else, and the background is the one the selected row uses. **Result: the marked row's computed style differs from a plain row's in its background and in nothing else -- colour and weight are asserted equal -- and the background is read from the selected row's own component style rather than named here, so the two cannot drift apart.**
- [x] 1.3 Check the bar covers the whole row rather than only where there is text. Verify in `t_mark.py` by rendering a marked row whose cells are shorter than their columns and asserting the background is present in the padding between two columns, not only under the characters. This is the check that would have caught building the bar out of cell styles instead. **Result: the rendered line for a marked row carries exactly one background across its whole width, and it is the bar's; a plain row's line does not carry it. Read through the table's own rendering, so the padding between columns is included -- which is what a bar built from cell styles would have left unpainted.**
- [x] 1.4 Confirm the bar is visible in both themes. Verify in `t_mark.py`: with the board on each theme in turn, the marked row's background differs from the view's own ground. Passing is a difference in both, not a fixed colour in either — the themes have different grounds and a check naming one would pass by luck on the other. **Result: the bar differs from the ground in both themes. The check needed a third row to park the cursor on: marking steps the cursor onto the next row, which then wears the cursor's own bar -- the very colour the check looks for on a row that should not have it. The first run failed in both themes for exactly that reason.**

## 2. Keep the row readable on the bar

- [x] 2.1 Leave the past-due colour and the dimming off a title while its row is marked. Verify in `t_mark.py` by reading the drawn cell's styles: a marked past-due row carries no colour of its own, and the same row unmarked still does. **Result: measured before changing anything -- a marked past-due title rendered at 1.30 to 1 against the bar, against 2.68 to 1 for an ordinary one, so the defect the design predicted was real. Now a marked past-due row's title carries the same colour as an ordinary row's, still shows how overdue it is, and an unmarked one is unchanged.**
- [x] 2.2 Keep the styles that are not colours. Verify in `t_mark.py`: a finished task and a mail row whose issue is finished are both still struck through while marked, and the strike is gone from neither. **Result: a marked finished task is still struck through and is no longer dimmed; unmarked it is both. The same drop was applied to a mail row whose issue is finished and to a calendar event that has ended -- the spec named only the first two, and was amended to say so rather than leaving the event dimmed to 1.2 to 1 on the bar.**
- [x] 2.3 Confirm an unmarked row is untouched by all of this. Verify by running `t_mailview.py` and `t_fit.py` whole — between them they hold the existing checks on the past-due colour, the dimming and the strike, and none may change. **Result: `t_mailview` 107/107 and `t_fit` 37/37, both unchanged.**

## 3. Tell the cursor from a mark

- [x] 3.1 Confirm the selected row still wins on a row that is both. Verify in `t_mark.py`: the style computed for a marked row under the cursor is the selected row's, and it differs from the style of a marked row the cursor is not on. **Result: a marked row under the cursor is drawn as the selected row; the two share the bar and differ in the text.**
- [x] 3.2 Keep the mark's own character on that row. Verify in `t_mark.py`: the row-mark cell of a marked row carries the mark character whether or not the cursor is on it. This is what makes a press of the key on the last row visible, so it is checked there too. **Result: marking the last row leaves the cursor on it and the mark character says so; pressing again takes it off, visibly. Without the character that press would have no visible answer at all, the cursor having nowhere to go.**
- [x] 3.3 Confirm a marked row and the selected row are drawn differently. Verify in `t_mark.py` by comparing the two computed styles: they share a background and differ in weight and text colour. Passing is a named difference, not merely inequality — two styles differing in something invisible would pass an inequality check and fail a person. **Result: they share a background, carry two different text colours, and the cursor's is the bold one -- each asserted by name rather than by inequality.**

## 4. Move the cursor on

- [x] 4.1 Move the selection to the next row when the marking key is pressed, before the redraw rather than after. Verify in `t_mark.py`: pressing the key once marks the row it was pressed on and leaves the cursor one row below it. **Result: done, set before the redraw. The first run of this group failed on all of it because the fixtures had no stored order, so the view fell back to ordering them by title and "the next row" was not the one the checks assumed. Fixtures now set their own order.**
- [x] 4.2 Mark a run with repeated presses. Verify in `t_mark.py`: four presses from the top mark the first four rows in order and leave the cursor on the fifth, and no row is marked twice. **Result: four presses from the top mark the first four in order and leave the cursor on the fifth, with no duplicate.**
- [x] 4.3 Move on when a mark comes off as well. Verify in `t_mark.py`: pressing the key on an already-marked row unmarks it and the cursor moves down, the same as when a mark goes on. **Result: confirmed. Two older checks had to change with it: pressing the key twice in place used to toggle a row and now marks two, which is the point of the change.**
- [x] 4.4 Stay put on the last row. Verify in `t_mark.py`: marking the last row of the view marks it and leaves the cursor on it, and pressing again unmarks it and still leaves the cursor on it. **Result: confirmed both ways.**
- [x] 4.5 Confirm the advance follows what is drawn rather than what is held. Verify in `t_mark.py` with a search in force that hides some rows: the cursor moves to the next row the view is drawing, not to a row the search has removed. **Result: with a search hiding the middle row, the cursor steps from the first drawn row to the third, over the one the search removed. The step reads the drawn list, so this holds for the work filter too by the same route.**

## 5. Guard the private hook

- [x] 5.1 Add a check that fails loudly if the framework's row-style hook goes. Verify in `t_version.py`, beside the existing checks on the framework: the hook exists on the framework's own table class and takes the arguments this board passes it. Passing is the check failing when the name is misspelt, which is confirmed by misspelling it once. **Result: the guard is in `t_mark.py`, not `t_version.py` as this task said -- that suite is about an issue's version field in the tracker and has nothing to do with the framework. The precedent followed is `t_crash.py`, which pins its own private hook the same way. Four checks: the hook exists on Textual's own class, the board overrides that name rather than adding a new one, the override calls through to it, and it still takes the same two arguments. Misspelling the name once made five drawing checks and the override check fail by name; the spelling was restored and the suite is back to 85/85.**

## 6. Checking the change as a whole

- [x] 6.1 Run the whole self-contained tier with `python tests/run.py`, with nothing else touching the repository during the run. Passing is every suite green and exit 0. A tier run sharing the repository is not evidence. **Result: 51 suites passed, 0 failed, 0 known to fail, exit 0. The tier was run alone, after the vacuity runs below had been undone and the tree checked back to its own checksum -- an earlier run overlapping a neutered build is what this task exists to rule out.**
- [x] 6.2 Take a checksum of `main.py` before that run and diff it afterwards. Passing is identical. Un-check any task whose verification predates a later edit to the code it verified. **Result: `main.py` read `b122204cf893e045793ed7019b5786a0323753e8` before the run and the same after; `tests/stub/t_mark.py` read `351dd20181bf710ac1b9f7a8adc700c68eadd2cd` both times. Nothing was un-checked: every earlier result in this file was measured against this same `main.py`, and the two edits made after them -- the neutering in 6.3 -- were each undone before the next measurement.**
- [x] 6.3 Show the new checks are not vacuous. Remove the background from the row-style hook, run the tier, and record which checks fail and in which suites. Then stop the cursor advancing and record the same. Passing is a named list for each, not a count — and if either list is short, the checks are weaker than they look and are to be strengthened rather than accepted. **Result: both halves measured; every failure fell in `t_mark.py` and no other suite noticed either removal, which is itself worth recording -- the drawing and the advance are held up by this suite alone.**

  With the background removed from the row-style hook (the hook returning the framework's own style unchanged), `t_mark` 81/87, six named failures:
  - the marked row's computed style differs from a plain one's
  - and differs in its background
  - which is the one the selected row uses
  - and it is the bar's
  - and the bar is painted under it
  - they share the bar

  With the cursor advance removed (the block that finds the row and steps past it replaced with `pass`), `t_mark` 82/87, five named failures:
  - one press marks the row and steps down
  - four presses mark the first four, in order
  - and leave the cursor on the fifth
  - and the cursor moved on just the same
  - the cursor stepped over the row the search removed

  Neither list is short, and each names the guarantee it lost rather than a count: the first covers the bar's existence, its colour, its source and its width; the second covers a single press, a run of presses, an unmarking press and the advance over a hidden row. One earlier check did prove vacuous before this run and was strengthened then rather than accepted -- "exactly one background" held whether that background was the bar or the ground, so it now finds the whitespace-only segments and asserts the bar is painted under them.**

## 7. At the board, by hand

- [x] 7.1 Open the board with the real data, mark a run of rows by holding the key, and look at it. Passing is the bar reading as a bar, the cursor findable inside the run, and the count on the status line agreeing with the number of bars on screen. **Result: confirmed at the board, on the real data. This is the check the suites cannot make: every drawing check above reads colours back out of the framework, and none of them can say whether a run of bars reads as a run to a person looking at it.**
- [x] 7.2 Mark a past-due row and a finished row and confirm both are still readable on the bar, in whichever theme is in use. Say which theme it was: the two have different grounds and only one can be looked at at a time. **Result: confirmed on the dark theme -- the board opens on it and nothing persists a change, so an unaltered launch is dark. The blue theme is covered only by measurement, not by a person's eye: 1.4 asserts the bar differs from the ground on both, and 2.1 measured the past-due colour at 1.30 to 1 on the bar before it was dropped, which is what this task was set to catch. If the blue theme ever comes into use, look again rather than reading this line as covering it.**
