## 1. The instrument, before anything is edited

- [x] 1.1 Run `python tests/run.py` with no product file changed and save every suite's `N/M checks passed` line to the session scratchpad as the baseline. Passing: the run is green and the file exists. A later count that has dropped is a lost check, not a tidied one, and this is the only thing that will say so.
- [x] 1.2 Note beside the baseline that `t_elapsed`'s total moves with the clock -- 36 checks by day, 31 in the last hour -- so that a change in its count is checked against the hour before it is believed.

## 2. The store

- [x] 2.1 Add `archived_at` (the store's archive date, parsed) and `archived` to the task model in `singularity.py`. Verify in the new stub suite `tests/stub/t_arch.py`: a synthetic raw task carrying an archive date answers it as a datetime in the local zone, one without answers None, and a malformed value answers None rather than raising.
- [x] 2.2 Add the archived-task query to `SingularityClient`, paging through `iter_tasks` with `includeArchived` and the archive-date filter set, dropping rows that are neither completed nor cancelled. Verify in `tests/stub/t_arch.py` against a stubbed client that answers three pages: every page's rows arrive, paging stops when the store says the total is reached, and the unfinished rows are not in the result.
- [x] 2.3 Verify the query against the real store once, with a script run by hand under the suites' token (`SINGULARITY_TEST_TOKEN`), not the board's. Passing: the row count it reports equals the total the store's pagination block gives for the same filter, and every row it kept is completed or cancelled. Counts only -- no title, project or date of any row is printed or recorded.

## 3. The position

- [x] 3.1 Introduce the archive as a position of its own in `singularity.py` -- a singleton with a label, not a third `Bucket` member -- and widen the position type to admit it. Verify in `tests/stub/t_arch.py`: the archive is not a `Bucket`, iterating `Bucket` still yields exactly the inbox and someday, and the archive answers a label.
- [x] 3.2 Read each of the nine `isinstance(position, Bucket)` sites in `main.py`, `board.py` and `singularity.py` and decide which of the two things it means -- "no calendar day" or "one of the two dateless buckets" -- changing only the first. Verify: the stub tier's counts are unchanged against the 1.1 baseline, with `t_search`, `t_add`, `t_label2`, `t_top` and `t_still` named as the ones that exercise views.
- [x] 3.3 Route the archive in `tasks_at` so that asking for the shown view's tasks answers for it. Verify in `tests/stub/t_arch.py`: asking a stubbed client for the archive returns the archived rows and asking for a day or a bucket is unchanged.

## 4. What is drawn

- [x] 4.1 Give `board.Board` the held archive and how its fetch stands, as values handed in rather than fetched. Verify in `tests/stub/t_arch.py`: a board is built with an archive of synthetic rows and painted with no user interface started, and the suite asserts `"main" not in sys.modules` at its end, as `t_paint` does.
- [x] 4.2 Draw the archive's rows: completed and cancelled only, most recently archived first, ties by title. Verify in `tests/stub/t_arch.py`: an archive whose rows are handed over in scrambled order paints in archive-date order; rows sharing a date paint in title order; a row that is archived but neither completed nor cancelled is not painted; a tagged row is not lifted above an untagged one.
- [x] 4.3 Bound what is drawn to the most recent 500 while holding every row, and match a search against every held row rather than the drawn ones. Verify in `tests/stub/t_arch.py`: an archive of 1,200 synthetic rows paints 500; the 900th-newest row is not painted but is painted when searched for; a search matching 1,100 rows paints 500 of them, the newest.
- [x] 4.4 Put the archive date in the `When` cell, formatted to the column's 11 characters, and keep the mark and strike a finished task already carries. Verify in `tests/stub/t_arch.py`: the cell reads as a date, is no wider than the column, and a completed and a cancelled row carry the marks they carry in every other view.
- [x] 4.5 Report in the status line how many rows the archive holds, how many are drawn, how many a search matched, and that a fetch is still running or has failed. Verify in `tests/stub/t_arch.py` by asserting the status text for five states: loading, loaded with more held than drawn, loaded with fewer, searched, and failed.

## 5. The board

- [x] 5.1 Bind `A` to the archive, with its Russian twin, and name it in the help. Verify: `t_twins2` passes with its count unchanged from the 1.1 baseline -- it is the suite that asserts every binding answers both layouts -- and `t_fit` passes, which is the one that asserts the key bar fits the terminal.
- [x] 5.2 Fetch the archive on a worker the first time the view is asked for, painting each page as it arrives, and hold it for the session. Verify in a new part of `tests/stub/t_arch.py` driven through the test pilot against a stubbed client that answers pages slowly: rows appear before the last page, the board answers keys while it runs, returning to the view does not fetch again, and a reload does.
- [x] 5.3 Refuse every write while the archive is shown, at the head of `refuse_foreign` so that it covers the actions added later, and check it is the view being refused rather than the row. Verify in `tests/stub/t_arch.py`: each writing key in the archive says so and sends nothing to the stubbed client; the same task selected on its own calendar day still ticks; undo after a refused key reverses nothing. **Superseded by 5.6/5.7:** the funnel cannot know the action and is reached after a set has been reported; the gate moved to `rows_for_write(action)` and the two keys that bypass it, and two writes are now allowed.
- [x] 5.4 Make adding a task, moving between days and reordering inert in the archive, each saying what it is for. Verify in `tests/stub/t_arch.py`: the add key creates nothing, the day keys leave the archive displayed, `t` returns to today, and the move keys report what they apply to.
- [x] 5.5 Report a failed fetch as the archive's failure and let asking again retry it. Verify in `tests/stub/t_arch.py` with a stubbed client that raises: the board says the archive could not be read, a calendar day still loads afterwards, and asking for the archive again calls the client a second time.
- [x] 5.6 Let `space` un-tick a row in the archive and `d` date it -- single row or marked set -- with undo reaching both, and keep the written-to row on screen until the archive is re-read. Verify in `tests/stub/t_arch.py`: the stubbed client records `set_done(False)` and `set_schedule(today)`; the row stays drawn, as unfinished; two undos restore date then tick; a marked pair is brought back together and the board says "Brought back 2"; a reload leaves them out.
- [x] 5.7 Refuse every other write by the key's name, before anything is announced, in `rows_for_write(action)` and in the two single-row keys that bypass it. Verify in `tests/stub/t_arch.py` with a bare cursor and with three rows marked: six keys each say only un-ticking and dating change a row here, none is reported as done, nothing is sent.
- [x] 5.8 Verify what bringing a task back takes against the real store, on one `zz-` task under the suites' token, deleted in a `finally`. Passing: `uncomplete` clears the archive date without a second call, and the task is on its own day and counted past due. **Done:** it does; probe recorded in the design.

## 6. Verification against the stub

- [x] 6.1 Run `tests/stub/t_arch.py` on its own under pytest and confirm every part it declares is collected and reported. Passing: the parts pytest lists equal the suite's `PARTS`, and the suite's `N/M checks passed` line accounts for every check it makes.
- [x] 6.2 Run the whole self-contained tier with `python tests/run.py` and compare every suite's count with the 1.1 baseline. Passing: no suite's count is lower, and any suite whose count rose is one this change added checks to. No credentials and no network are involved.
- [x] 6.3 Freeze the product files for that run -- `shasum` before and after -- so that the comparison is of one code state, and check any failure against a worktree at HEAD before calling it a regression.

## 7. Verification against the live account

- [x] 7.1 Open the board against the real account with the board's own token, press `A`, and confirm by hand: the view fills while it loads, says so while it does, and stops saying so when it is done. Report the counts it shows and nothing else -- no title, project, sender or date of any row. **Confirmed by hand** against the real account; nothing about any row was reported.
- [x] 7.2 By hand in the same session: search the archive for a word, confirm a row older than the 500 drawn is found by it; mark two rows and copy them with `y` and `Y`; press a writing key and confirm the refusal; press `r` and confirm it fetches again. **First pass found two things.** With rows marked, `space` reported "Brought back 3" while every row was refused (nothing written; fixed in 5.7). And the view was read-only, which the user had not asked for -- restoring is what they wanted; settled as un-tick and date (5.6). **Left to confirm by hand:** `space` on an archived row brings it back and the row stays, drawn unticked; `d` then sends it to today; `x` says only un-ticking and dating change a row here. **Confirmed by hand by the user after the fix: works fine.**
- [x] 7.3 Run the store tier once, `python tests/run.py --store`, under the suites' token rather than the board's, paced as the runner paces it. Passing: its counts match the 1.1 baseline, confirming the position change broke nothing that talks to the real store. **Done differently:** the 1.1 baseline covers the self-contained tier only, so the comparison was against a git worktree at HEAD, run the same way minutes apart. Both runs: 8 passed, 10 failed, suite for suite and check for check (t_green6 10/11, t_label 24/25, eight with no checks line). The ten failures are therefore pre-existing. At least one cause is named by the suite itself: the day-guarded capture in `tests/local/baseline.json` describes 2026-09-15 and today is 2026-09-17, which I should have refreshed before starting and did not.

## 8. What is published

- [x] 8.1 Add the archive to `README.md`: a line in the feature list saying what the view is and that it is read-only, and the key in the table. Passing: the README names `A` and the file's key table and feature list agree with the bindings in `main.py`. **Done differently:** the README has no key table -- it names keys inline and says `?` shows them all in the board -- so `A` is named inline beside `h`, `l`, `t`, `i` and `s`, and no table was invented for it.
- [x] 8.2 Add an archive board to `SHOTS` in `docs/screenshots.py` and regenerate, so the new view is shown with the others. Passing: `docs/archive.png` is produced from invented data by one run of the script, and the README shows it.
