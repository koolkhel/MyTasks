## 1. Find out what the present checks already say

The selected row is drawn by a rule several suites read. Knowing which before
changing it is what keeps a green run meaningful.

- [x] 1.1 List every stub suite that asserts something about the selected row's colours or the marked row's bar. Read them and record which assert a value and which compare two values. Passing is a written list, because a check naming a colour will pass on one theme and say nothing about the other, and those are the checks this change has to rewrite rather than satisfy. **Result: two suites touch row colours. `t_mark.py` holds all of them, and one must be rewritten -- "which is the one the selected row uses" asserts the marked bar *equals* the selected row's, which is the defect this change fixes. Its neighbours compare rather than name and survive: the marked style differing from a plain one's, one background across the whole line, the bar under the padding, and the bar differing from the ground in each theme. `t_elapsed.py` checks the header's contrast in both themes and names no row colour; it also already carries a luminance helper, whose shape the new checks reuse.**
- [x] 1.2 Record, before any edit, what the two rows measure today: the selected row's text against its bar, a marked row's text against its bar, and each bar against each theme's ground. Take them from the running board rather than from the design, so the after-figures are compared with something measured on the same machine. **Result, measured from the running board in both themes. Selected row: bar `#00aaaa`, text `#ddf3f3`, **2.48:1**. Marked row: bar `#00aaaa` -- confirmed identical to the selected row's. Bars against the ground: 7.33:1 on the dark theme, 4.64:1 on the blue. Both themes give the same two figures for the rows, differing only in the ground.**

## 2. The two colours

- [x] 2.1 Give the selected row the brighter bar the themes declare, with dark text. Verify in `t_mark.py` by reading the style the table computes for the selected row: its background is the brighter value and its foreground is the darker of the theme's two, in both themes. **Result: the stylesheet rule now reads `background: $primary; color: $background`. Measured from the running board: dark theme `#55ffff` bar with `#000000` text, **17.13:1**, up from 2.48:1; blue theme the same bar with `#0000aa` text, **10.84:1**. Checked by comparing luminance rather than naming a colour, so one form of words covers both themes.**
- [x] 2.2 Stop the row-style hook reading the marked bar off the selected row's component style, and give it a value of its own. Verify in `t_mark.py`: the style computed for a marked row and the style computed for the selected row have different backgrounds. This is the check that matters most — leaving the hook as it is breaks nothing and silently keeps the two identical. **Result: `TaskTable` now declares its own `tasktable--marked` component class, set in the stylesheet beside the selected row's, and the hook reads that. The check that mattered was already in the suite asserting the opposite -- "which is the one the selected row uses" -- and it failed on the first run, which is what a check enforcing a defect looks like. Rewritten to assert the two differ. A second check, "they share the bar", failed the same way and was rewritten too.**
- [x] 2.3 Confirm the selected row's bar is the brighter of the two. Verify in `t_mark.py` by computing the relative luminance of both backgrounds and comparing them. Passing is the comparison holding in both themes, with no colour named in the check. **Result: the comparison holds in both themes, and the two bars are 2.34:1 apart -- checked against a floor of 2:1, so making them nearly the same would fail rather than pass.**

## 3. What must not change

- [x] 3.1 Confirm a marked row is drawn exactly as it was. Verify by running `t_mark.py` whole — it holds the existing checks on the bar covering the full row, the mark character surviving under the cursor, the suppressed past-due colour and the kept strikethrough, and none of them may change. **Result: `t_mark.py` 102/102. It was 87 before this change: two checks were rewritten because they asserted the two bars were the same, and fifteen were added. Every existing check about the marked row itself -- the bar covering the padding, the mark character under the cursor, the past-due colour left off, the strike kept -- passes unchanged.**
- [x] 3.2 Confirm nothing about marking's behaviour moved. Verify by running `t_copy.py` and `t_pastein.py` whole: what is marked, what is copied, where the cursor goes and what a paste does are untouched by a colour. **Result: `t_copy.py` 43/43 and `t_pastein.py` 50/50, both unchanged.**
- [x] 3.3 Confirm the rest of the board's drawing is untouched. Verify by running `t_fit.py`, `t_fit2.py`, `t_still.py` and `t_mailview.py` whole — between them they hold the column widths, the redraw position and the row contents. **Result: `t_fit.py` 37/37, `t_fit2.py` 17/17, `t_still.py` 40/40, `t_mailview.py` 107/107 -- all unchanged.**

## 4. Both themes

- [x] 4.1 Check every distinction the theme requirement names still holds under both themes, with rows marked. Verify in `t_mark.py`: with the rows above and below the selected one marked, the selected row's background differs from theirs and from the ground, under each theme in turn. Passing is both themes, compared rather than named. **Result: with the rows above and below marked, the selected row's bar is in neither of theirs, in both themes. It also differs from the ground by 17.13:1 on the dark theme and 10.84:1 on the blue -- so it is findable with nothing marked at all, which it was not before: both bars used to sit at 7.33:1 and 4.64:1.**
- [x] 4.2 Check the text on each bar is legible against it by the board's own rule — dark text on a light bar, light on a dark one. Verify in `t_mark.py` by comparing the luminance of each bar with the luminance of the text drawn on it, under both themes. **Result: the selected row's bar is the lighter of bar and text in both themes, which is the board's own rule, and its text reads at 17.13:1 and 10.84:1. A marked row's title still reads on its own bar at 2.68:1 -- unchanged, because nothing about the marked row moved.**

## 5. Checking the change as a whole

- [x] 5.1 Run the whole self-contained tier with `python tests/run.py`, with nothing else touching the repository during the run. Passing is every suite green and exit 0. A tier run sharing the repository is not evidence. **Result: 51 suites passed, 0 failed, exit 0, nothing else touching the repository during the run. Run twice: once on the two-colour version, and again after 5.3 found the text colour was not earning its place and it was dropped. The figures here are the second run, which is the code that ships.**
- [x] 5.2 Take a checksum of `main.py` before that run and diff it afterwards. Passing is identical. Un-check any task whose verification predates a later edit to the code it verified. **Result: `49a42b2e16` before and after the final run -- identical. The earlier run was at `ffe7bf4625`, and 5.3 then changed the code, so that run's evidence was superseded rather than kept: the tier was run again whole at the shipping checksum and every task above re-checked against it.**
- [x] 5.3 Show the new checks are not vacuous. Put the marked row's bar back to reading the selected row's style, run the tier, and record which checks fail and where. Then set the selected row's text back to the theme's resolved colour and record the same. Passing is a named list for each; a short list means the checks are weaker than they look and are to be strengthened rather than accepted. **Result, marked bar put back to reading the selected row's style: 9 checks fail -- the marked bar not being the selected row's, the selected row's being brighter, the two being far enough apart (reported at 1.00:1), the two not sharing a bar, the cursor wearing the brighter one, the selected row differing from its marked neighbours in each theme, and a marked row's title still reading on its bar in each theme (1.15:1). The defect is caught nine ways.

  Result, selected row's text put back to the theme's resolved colour: **0 checks fail**, and that turned out to be right rather than a gap. Measured, the resolved colour gives 13.67:1 in both themes where naming the ground gives 17.13:1 under the dark theme and 10.84:1 under the blue -- better on one, worse on the other. The 2.48:1 the change set out to fix was the resolved colour against the *mid-tone* bar, not resolving as such; against the brighter bar it resolves well on its own. So the second colour was dropped and the change is one colour rather than two. The design and the proposal were corrected to say so, and what makes deriving safe is the check that the bar is the lighter of bar and text and that the two are at least 4.5:1 apart.**
- [x] 5.4 Measure the two rows again and compare with 1.2. Passing is the selected row's text improving against its bar, the marked row's figures unchanged, and both bars still separated from each theme's ground. **Result, against 1.2's figures. Selected row's text on its bar: **2.48:1 -> 13.67:1** in both themes. Marked row: bar still `#00aaaa`, title still 2.68:1 on it, unchanged. Bars against the ground: the marked one is unchanged at 7.33:1 dark and 4.64:1 blue; the selected row's went from those same figures to 17.13:1 and 10.84:1, so it is now findable with nothing marked as well. And the two bars are no longer the same colour, which is the thing 1.2 recorded as the defect.**

## 6. At the board, by hand

- [x] 6.1 Open the board in the terminal actually used, mark a run of rows, and put the cursor inside it. Passing is the cursor findable at a glance without reading any text. Say which terminal: the ratios are computed from the colours the themes declare, and a terminal with its own palette for those colours can paint either bar differently. **Result: confirmed at the board, in kitty -- the same terminal the complaint came from, where the cursor was reported as hard to see before this. Reported as much better, and found without reading any text.**
- [x] 6.2 Look at the same thing with nothing marked, and on the other theme. Passing is the cursor still obvious in both, and the brighter bar not being so loud in use that it is worse than what it replaced — which is a judgement only the person using it can make, and the design names the smaller alternative to fall back to if the answer is that it is. **Result: confirmed on both themes by eye, and the brighter bar is right in use rather than too loud -- so it stays. The quieter fallback the design names (dark text on the bar the cursor had, 7.33:1) is not taken, and the reason it exists is recorded there should the judgement change.**
